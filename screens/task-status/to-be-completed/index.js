import React from 'react';
import { Content } from 'components';
import { Text, View } from 'react-native';

const ToBeCompleted = ({ params }) => (
    <Content>
        <Text>ToBeCompleted</Text>
    </Content>
);

export default ToBeCompleted;
