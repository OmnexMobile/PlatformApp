diff --git a/node_modules/react-native/ReactAndroid/src/main/java/com/facebook/react/fabric/mounting/SurfaceMountingManager.java b/node_modules/react-native/ReactAndroid/src/main/java/com/facebook/react/fabric/mounting/SurfaceMountingManager.java
index 8d0f5a1..4d2a8a1 100644
--- a/node_modules/react-native/ReactAndroid/src/main/java/com/facebook/react/fabric/mounting/SurfaceMountingManager.java
+++ b/node_modules/react-native/ReactAndroid/src/main/java/com/facebook/react/fabric/mounting/SurfaceMountingManager.java
@@ -786,7 +786,10 @@ public class SurfaceMountingManager {
       return;
     }
 
-    ViewState viewState = getViewState(reactTag);
+    ViewState viewState = getNullableViewStateOrLog(reactTag, "updateProps");
+    if (viewState == null) {
+      return;
+    }
 
     if (ReactNativeFeatureFlags.overrideBySynchronousMountPropsAtMountingAndroid()
         && !shouldSkipSynchronousMountPropsOverride
@@ -899,7 +902,10 @@ public class SurfaceMountingManager {
       return;
     }
 
-    ViewState viewState = getViewState(reactTag);
+    ViewState viewState = getNullableViewStateOrLog(reactTag, "sendAccessibilityEvent");
+    if (viewState == null) {
+      return;
+    }
 
     if (viewState.mViewManager == null) {
       throw new RetryableMountingLayerException(
@@ -925,8 +931,11 @@ public class SurfaceMountingManager {
       return;
     }
 
-    ViewState viewState = getViewState(reactTag);
+    ViewState viewState = getNullableViewStateOrLog(reactTag, "updateLayout");
+    if (viewState == null) {
+      return;
+    }
     // Do not layout Root Views
     if (viewState.mIsRoot) {
       return;
     }
@@ -1001,7 +1010,10 @@ public class SurfaceMountingManager {
       return;
     }
 
-    ViewState viewState = getViewState(reactTag);
+    ViewState viewState = getNullableViewStateOrLog(reactTag, "updatePadding");
+    if (viewState == null) {
+      return;
+    }
     // Do not layout Root Views
     if (viewState.mIsRoot) {
       return;
@@ -1035,7 +1047,10 @@ public class SurfaceMountingManager {
       return;
     }
 
-    ViewState viewState = getViewState(reactTag);
+    ViewState viewState = getNullableViewStateOrLog(reactTag, "updateOverflowInset");
+    if (viewState == null) {
+      return;
+    }
     // Do not layout Root Views
     if (viewState.mIsRoot) {
       return;
@@ -1063,7 +1078,10 @@ public class SurfaceMountingManager {
       return;
     }
 
-    ViewState viewState = getViewState(reactTag);
+    ViewState viewState = getNullableViewStateOrLog(reactTag, "updateState");
+    if (viewState == null) {
+      return;
+    }
 
     StateWrapper prevStateWrapper = viewState.mStateWrapper;
     viewState.mStateWrapper = stateWrapper;
@@ -1135,7 +1153,10 @@ public class SurfaceMountingManager {
       return;
     }
 
-    ViewState viewState = getViewState(reactTag);
+    ViewState viewState = getNullableViewStateOrLog(reactTag, "setJSResponder");
+    if (viewState == null) {
+      return;
+    }
     View view = viewState.mView;
     if (initialReactTag != reactTag && view instanceof ViewParent) {
       // In this case, initialReactTag corresponds to a virtual/layout-only View, and we already
@@ -1277,6 +1298,17 @@ public class SurfaceMountingManager {
     return viewStates.get(tag);
   }
 
+  private @Nullable ViewState getNullableViewStateOrLog(int tag, String operation) {
+    ViewState viewState = getNullableViewState(tag);
+    if (viewState == null) {
+      ReactSoftExceptionLogger.logSoftException(
+          ReactSoftExceptionLogger.Categories.SURFACE_MOUNTING_MANAGER_MISSING_VIEWSTATE,
+          new ReactNoCrashSoftException(
+              "Unable to find viewState for tag: " + tag + " for " + operation));
+    }
+    return viewState;
+  }
+
   @SuppressWarnings("unchecked") // prevents unchecked conversion warn of the <ViewGroup> type
   private static @NonNull IViewGroupManager<ViewGroup> getViewGroupManager(
       @NonNull ViewState viewState) {
diff --git a/node_modules/react-native/ReactAndroid/build.gradle.kts b/node_modules/react-native/ReactAndroid/build.gradle.kts
index 1eaa544..b094d7c 100644
--- a/node_modules/react-native/ReactAndroid/build.gradle.kts
+++ b/node_modules/react-native/ReactAndroid/build.gradle.kts
@@ -49,7 +49,7 @@ extra["publishing_version"] = project.findProperty("VERSION_NAME")?.toString()!!
 // This is the version of CMake we're requesting to the Android SDK to use.
 // If missing it will be downloaded automatically. Only CMake versions shipped with the
 // Android SDK are supported (you can find them listed in the SDK Manager of Android Studio).
-val cmakeVersion = System.getenv("CMAKE_VERSION") ?: "3.30.5"
+val cmakeVersion = System.getenv("CMAKE_VERSION") ?: "3.22.1"
 
 extra["cmake_version"] = cmakeVersion
 
@@ -568,6 +568,7 @@ android {
             "-DANDROID_STL=c++_shared",
             "-DANDROID_TOOLCHAIN=clang",
             "-DANDROID_SUPPORT_FLEXIBLE_PAGE_SIZES=ON",
+            "-DCMAKE_INTERPROCEDURAL_OPTIMIZATION=False",
             "-DCMAKE_POLICY_DEFAULT_CMP0069=NEW",
         )
 
diff --git a/node_modules/react-native/ReactAndroid/hermes-engine/build.gradle.kts b/node_modules/react-native/ReactAndroid/hermes-engine/build.gradle.kts
index 49b0d7e..b864fce 100644
--- a/node_modules/react-native/ReactAndroid/hermes-engine/build.gradle.kts
+++ b/node_modules/react-native/ReactAndroid/hermes-engine/build.gradle.kts
@@ -144,8 +144,12 @@ val installCMake by
     tasks.registering(CustomExecTask::class) {
       onlyIfProvidedPathDoesNotExists.set(cmakePath)
-      commandLine(
-          windowsAwareCommandLine(getSDKManagerPath(), "--install", "cmake;${cmakeVersion}")
-      )
+      if (File(cmakePath).exists()) {
+        commandLine(windowsAwareCommandLine(cmakeBinaryPath, "--version"))
+      } else {
+        commandLine(
+            windowsAwareCommandLine(getSDKManagerPath(), "--install", "cmake;${cmakeVersion}")
+        )
+      }
     }
 
 fun configureBuildForHermesCommandLineArgs(
@@ -353,7 +353,7 @@ android {
             "-DJSI_DIR=${jsiDir}",
             "-DHERMES_BUILD_SHARED_JSI=True",
             "-DHERMES_RELEASE_VERSION=for RN ${version}",
-            "-DCMAKE_INTERPROCEDURAL_OPTIMIZATION=True",
+            "-DCMAKE_INTERPROCEDURAL_OPTIMIZATION=False",
             // We intentionally build Hermes with Intl support only. This is to simplify
             // the build setup and to avoid overcomplicating the build-type matrix.
             "-DHERMES_ENABLE_INTL=True",
diff --git a/node_modules/react-native/third-party-podspecs/RCT-Folly.podspec b/node_modules/react-native/third-party-podspecs/RCT-Folly.podspec
index 84fb7f2..15b634d 100644
--- a/node_modules/react-native/third-party-podspecs/RCT-Folly.podspec
+++ b/node_modules/react-native/third-party-podspecs/RCT-Folly.podspec
@@ -71,6 +71,7 @@ Pod::Spec.new do |spec|
                       'folly/net/*.h',
                       'folly/net/detail/*.h',
                       'folly/portability/*.h',
+                      'folly/synchronization/*.h',
                       'folly/system/*.h',
 
   # workaround for https://github.com/facebook/react-native/issues/14326
@@ -91,6 +92,7 @@ Pod::Spec.new do |spec|
                         'folly/net/*.h',
                         'folly/net/detail/*.h',
                         'folly/portability/*.h',
+                        'folly/synchronization/*.h',
                         'folly/system/*.h',
  spec.libraries           = "c++abi" # NOTE Apple-only: Keep c++abi here due to https://github.com/react-native-community/releases/issues/251
  spec.pod_target_xcconfig = { "USE_HEADERMAP" => "NO",
